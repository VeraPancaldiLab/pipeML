% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/machine_learning.R
\name{cross_validation_custom}
\alias{cross_validation_custom}
\title{Perform Nested Cross-Validation for Multiple Survival Models}
\usage{
cross_validation_custom(
  df_features,
  df_outcome,
  outcome_col,
  event_col,
  ml_options = list(nb_folds = 5, nb_repeats = 1, ncores = parallel::detectCores() - 1,
    LODO = FALSE, batch_id = NULL),
  file_name = NULL
)
}
\arguments{
\item{df_features}{A data frame (or tibble) containing predictor variables (X matrix).}

\item{df_outcome}{A data frame (or tibble) containing survival outcome columns:
typically survival time and event indicator.}

\item{outcome_col}{Character string specifying the name of the survival time column.}

\item{event_col}{Character string specifying the name of the event indicator column (0 = censored, 1 = event).}

\item{ml_options}{A named list of machine learning options controlling cross-validation and parallelization:
\describe{
\item{\code{nb_folds}}{Number of folds for v-fold cross-validation (default = 5).}
\item{\code{nb_repeats}}{Number of repetitions of cross-validation (default = 1).}
\item{\code{ncores}}{Number of CPU cores for parallel processing (default = \code{parallel::detectCores() - 1}).}
\item{\code{LODO}}{Logical; if \code{TRUE}, use Leave-One-Domain-Out stratification by cohort × event.}
\item{\code{batch_id}}{Character; name of the cohort or batch column (required if \code{LODO = TRUE}).}
}}

\item{file_name}{Optional character string specifying the file name suffix for
saving the generated C-index summary plot (PDF).}
}
\description{
This function performs \emph{nested cross-validation} with hyperparameter tuning
across multiple survival models using the tidymodels framework.
It supports both standard stratified cross-validation (by event rate)
and \emph{Leave-One-Domain-Out (LODO)} stratification, which balances by
cohort × event combinations.
}
\details{
For each model and hyperparameter configuration, the function:
\enumerate{
\item Trains the model across multiple folds
\item Computes the C-index (concordance index)
\item Aggregates median and MAD (robust variability measure) across folds
}
Finally, it identifies the best-performing model and hyperparameter
configuration and returns all results in a structured list.
}
