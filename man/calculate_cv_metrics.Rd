% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/machine_learning.R
\name{calculate_cv_metrics}
\alias{calculate_cv_metrics}
\title{Calculate Cross-Validated Metrics for Machine Learning Models}
\usage{
calculate_cv_metrics(ml_model, metric, hyperparameters = NULL)
}
\arguments{
\item{ml_model}{A trained machine learning model object containing \code{Prediction_folds} and \code{Resample_matrix}.}

\item{metric}{Character string specifying the metric to optimize when selecting hyperparameters. Typical values are \code{"AUROC"}, \code{"AUPRC"}, or \code{"Accuracy"}.}

\item{hyperparameters}{Optional character vector of hyperparameter column names to evaluate. If \code{NULL}, no hyperparameter tuning is performed.}
}
\value{
A list containing:
\describe{
\item{\code{Prediction_folds}}{Data frame of out-of-fold predictions with computed metrics for each resample and hyperparameter combination.}
\item{\code{Resample_matrix}}{Data frame of per-resample performance metrics (AUROC, AUPRC, Accuracy) for the tuned model.}
\item{\code{Results_folds}}{Aggregated performance metrics across hyperparameter combinations or resamples.}
\item{\code{bestTune}}{Optimal hyperparameter configuration based on the selected metric. Set to \code{"none"} if no hyperparameters are provided.}
}
}
\description{
This function computes cross-validated performance metrics for a trained machine learning model,
including AUROC, AUPRC, Accuracy, and other threshold-based metrics derived from out-of-fold predictions.
It also handles hyperparameter tuning by selecting the optimal parameter set based on a specified metric.
}
\details{
The function performs the following steps:
\enumerate{
\item Calculates AUROC and AUPRC for each resample in \code{Prediction_folds} using \code{calculate_auc_roc_resample()} and \code{calculate_auc_prc_resample()}.
\item Computes threshold-based metrics (Accuracy, Sensitivity, Specificity, Precision, F1-score, MCC) for each resample using \code{get_sensitivity_specificity()}.
\item If hyperparameters are provided:
\itemize{
\item Aggregates metrics across all resamples per hyperparameter combination.
\item Selects the best hyperparameters based on the \code{metric} argument.
\item Filters predictions and recomputes metrics for the tuned configuration.
}
\item If no hyperparameters are provided, aggregates metrics across all resamples.
\item Updates \code{Resample_matrix} and returns all relevant components.
}

This workflow ensures robust cross-validation evaluation and hyperparameter tuning in a reproducible way.
}
\keyword{internal}
