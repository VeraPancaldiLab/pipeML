% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/machine_learning.R
\name{machine_learning_custom}
\alias{machine_learning_custom}
\title{Train and Evaluate a Single Survival Model}
\usage{
machine_learning_custom(
  df_train,
  df_test,
  outcome_col,
  event_col = NULL,
  model,
  models_hyperparameters
)
}
\arguments{
\item{df_train}{A data frame (or tibble) containing training samples, including
survival time, event indicator, and predictor variables.}

\item{df_test}{A data frame (or tibble) containing test samples, structured
identically to \code{df_train}.}

\item{outcome_col}{Character string giving the name of the survival time column.}

\item{event_col}{Character string giving the name of the event indicator column
(1 = event occurred, 0 = censored).}

\item{model}{Character string specifying the model to train. Supported values include:
\itemize{
\item \code{"cox_ph_survival"} – Cox proportional hazards model ({survival})
\item \code{"proportional_hazards_glmnet"} – Penalized Cox model (LASSO/Elastic Net)
\item \code{"survreg_flexsurv"} – Parametric AFT model
\item \code{"rand_forest_partykit"} – Random survival forest using conditional inference trees
\item \code{"rand_forest_aorsf"} – Oblique random survival forest
\item \code{"decision_tree_partykit"} – Single survival tree
\item \code{"bag_tree_rpart"} – Bagged survival trees
\item \code{"boost_tree_mboost"} – Gradient boosting for censored data
}}

\item{models_hyperparameters}{A (possibly named) list containing hyperparameter
settings to apply to the selected model. Typically one element per model,
e.g. `list(list(trees = 500, min_n = 10}
}
\description{
This function trains and evaluates a single survival model (e.g., Cox, AFT,
Random Forest, Gradient Boosting, etc.) using the tidymodels framework.
It supports a variety of model engines, user-defined hyperparameters, and
computes the Concordance Index (C-index) as the primary evaluation metric.
}
\details{
The function automatically standardizes predictions from different engines,
ensuring that all models produce a comparable numeric risk score
(higher = higher risk). It gracefully handles differences in prediction
formats across models and adjusts the prediction direction when necessary.
}
